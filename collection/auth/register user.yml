info:
  name: register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{base}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "username":"Natasha Romanoff",
        "email": "natasha@gmail.com",
        "password":"natasha1646"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
