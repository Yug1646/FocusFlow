info:
  name: session by id
  type: http
  seq: 4

http:
  method: GET
  url: "{{base}}/api/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
