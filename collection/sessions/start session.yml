info:
  name: start session
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/api/sessions"
  body:
    type: json
    data: |-
      {
        "title":"Learning Typescript"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
