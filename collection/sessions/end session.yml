info:
  name: end session
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/api/sessions/end/6"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
