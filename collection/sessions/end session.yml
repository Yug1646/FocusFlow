info:
  name: end session
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base}}/api/sessions/111/end"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
