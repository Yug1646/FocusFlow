info:
  name: update session
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{base}}/api/sessions/20"
  body:
    type: json
    data: |-
      {
        "title":"Testing APIs"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
