info:
  name: delete session
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{base}}/api/sessions/15"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
