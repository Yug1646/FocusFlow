info:
  name: user by email
  type: http
  seq: 3

http:
  method: GET
  url: "{{base}}/api/users/by-email/yug@gmail.com"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
