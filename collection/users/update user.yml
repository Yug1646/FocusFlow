info:
  name: update user
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{base}}/api/users/1"
  body:
    type: json
    data: |-
      {
        "username":"Yug"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
