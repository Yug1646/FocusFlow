info:
  name: update user password
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{base}}/api/users/password"
  body:
    type: json
    data: |-
      {
        "oldPassword":"yug123",
        "newPassword":"yug1646"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
