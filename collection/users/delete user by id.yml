info:
  name: delete user by id
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base}}/api/users/4"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
