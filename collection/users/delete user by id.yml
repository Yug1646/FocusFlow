info:
  name: delete user by id
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base}}/api/users/8"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
