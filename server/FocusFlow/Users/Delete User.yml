info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base}}/api/users/5"
  auth: inherit

runtime:
  variables:
    - name: base
      value: http:/localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
