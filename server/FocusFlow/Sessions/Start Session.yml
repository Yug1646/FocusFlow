info:
  name: Start Session
  type: http
  seq: 2

http:
  method: POST
  url: "{{base}}/api/users/1/sessions"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: base
      value: http:/localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
