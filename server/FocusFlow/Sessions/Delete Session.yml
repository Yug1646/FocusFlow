info:
  name: Delete Session
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{base}}/api/users/1/sessions/2"
  body:
    type: json
    data: ""
  auth: inherit

runtime:
  variables:
    - name: base
      value: http:/localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
