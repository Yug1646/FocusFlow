info:
  name: Sessions
  type: http
  seq: 1

http:
  method: GET
  url: "{{base}}/api/sessions"
  auth: inherit

runtime:
  variables:
    - name: base
      value: http:/localhost:8000

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
